import type { ReactNode } from 'react';

import { ProfileScreen } from '@/features/profile';

/** C-29 — Profil & paramètres (réel : session + déconnexion). */
export default function Profile(): ReactNode {
  return <ProfileScreen />;
}
