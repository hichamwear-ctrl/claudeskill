import type { ReactNode } from 'react';

import { HomeScreen } from '@/features/intake';

/** C-07 — Accueil client (saisie libre du besoin → dialogue). */
export default function Home(): ReactNode {
  return <HomeScreen />;
}
